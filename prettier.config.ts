import { type Config } from 'prettier'

const config: Config = {
    arrowParens: "avoid",
    bracketSpacing: false,
    printWidth: 80,
    semi: false,
    singleQuote: true,
    tabWidth: 4,
    trailingComma: "es5",
}

export default config
